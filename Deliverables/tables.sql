create schema login;

create table Users(
Username varchar(45), 
Password varchar(45));

-- for first timers
-- users (kok, kim, king)
-- password (enter any new password)

create table Appointments(
date date, 
time time);