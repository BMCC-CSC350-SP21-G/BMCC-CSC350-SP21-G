create schema login;

create table Users(
UserId varchar(16), 
Password varchar(16));

-- for first timers
-- users (kok, kim, king)
-- password (enter any new password)

create table Appointments(
date date, 
time time);
